# ADR-0004: Формат возвращаемых результатов поиска

## Статус
Принято

## Контекст
Пользователи должны получать не только заголовки найденных разделов, но и релевантные фрагменты текста (цитаты) для быстрого понимания контекста. Разделы небольшие (в среднем ~300 слов), поэтому формат ответа должен быть простым и эффективным.

## Решение
Возвращать результаты поиска с цитатами (фрагментами текста) в следующем формате:

### Структура результата
```python
{
    "section_title": "Заголовок раздела",
    "score": 0.85,  # Косинусное сходство (0-1)
    "text": "Фрагмент текста раздела...",  # Цитата
    "full_text": "..."  # Опционально: полный текст для больших разделов
}
```

### Логика выбора фрагмента
- **Если раздел небольшой** (< 500 символов): возвращается весь текст раздела
- **Если раздел большой**: возвращается snippet — первые 200-300 символов с указанием заголовка

### Формат ответа пользователю
```
🔍 Найдено разделов: 3

📌 *Заголовок раздела 1*
Текст раздела или фрагмент...

──────────────────────────────

📌 *Заголовок раздела 2*
Текст раздела или фрагмент...
```

## Последствия

### Положительные
- ✅ Пользователи сразу видят релевантный контент
- ✅ Не нужно открывать полный раздел для понимания
- ✅ Простой формат, легко читается в Telegram

### Отрицательные
- ❌ Для MVP не реализовано выделение конкретного релевантного места в разделе
- ❌ Snippet может не содержать самую релевантную часть (решается в будущем)

### Будущие улучшения
- Подсветка конкретного места в разделе, которое наиболее релевантно запросу
- Анализ подобия фраз для более точного выделения релевантных фрагментов

## Альтернативы

### Отклонено: Возврат только заголовков
- **Почему**: недостаточно информации для пользователя, требуется дополнительный запрос

### Отклонено: Возврат только полного текста
- **Почему**: для больших разделов избыточно, перегружает ответ

### Отклонено: Сложное форматирование с выделением
- **Почему**: избыточно для MVP, усложняет реализацию

## Связанные решения
- [ADR-0001: Использование семантического поиска](./0001-semantic-search-via-embeddings.md)

## Дата
2026-01-16
