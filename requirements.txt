# Telegram Bot API
python-telegram-bot>=20.0

# Google Docs API
google-api-python-client>=2.0.0
google-auth>=2.0.0

# Конфигурация
pyyaml>=6.0

# Утилиты
python-dotenv>=1.0.0
html2text>=2020.1.16

# Embedding models для семантического поиска
sentence-transformers>=2.2.0
# torch: не указываем здесь. В Docker ставится CPU-only сборка (см. Dockerfile).
# Локально: pip сам подтянет torch через sentence-transformers (по умолчанию CUDA-сборка с PyPI).

